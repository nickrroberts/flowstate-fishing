{{!< default}}

{{!-- Services Page Template --}}
<div class="services-page">
    {{!-- Hero Section for Services Page --}}
    <section class="services-page-hero">
        <div class="gh-container">
            <div class="services-page-header">
                <h1 class="services-page-title">Our Services</h1>
                <p class="services-page-subtitle">Professional Fly Casting Lessons for Every Level</p>
            </div>
        </div>
    </section>

    {{!-- Services Section --}}
    <section id="services" class="services-section">
        <div class="gh-container">
            {{> "services"}}
        </div>
    </section>
</div>

<style>
    .services-page {
        padding-top: 100px; /* Account for fixed navbar */
    }
    
    .services-page-hero {
        background: linear-gradient(135deg, #FFD700 0%, #FFA500 25%, #FF4500 50%, #DC143C 75%, #8B0000 100%);
        padding: 4rem 0 3rem;
        text-align: center;
    }
    
    .services-page-title {
        font-family: var(--font-display);
        font-size: clamp(2.5rem, 5vw, 4rem);
        color: white;
        margin-bottom: 1rem;
        text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
    }
    
    .services-page-subtitle {
        font-size: 1.25rem;
        color: rgba(255, 255, 255, 0.95);
        max-width: 600px;
        margin: 0 auto;
    }
</style>