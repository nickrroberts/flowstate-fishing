<div class="gallery-header">
  <div class="badge-wrapper">
    <span class="gallery-badge">Trophy Shots</span>
  </div>
  <h2 class="gallery-title">
    Fish Pics or
    <span class="gallery-subtitle gallery-underline-svg">It Didn't Happen</span>
  </h2>
  <p class="gallery-description">
    {{#if @custom.gallery_subtitle}}{{@custom.gallery_subtitle}}{{else}}Check out these absolute units we've yanked out of the water. Fair warning: viewing these pics may cause sudden urges to quit your job and go fishing.{{/if}}
  </p>
</div>

{{!-- Gallery Grid using posts tagged gallery --}}
<div class="gallery-grid">
  {{#get "posts" filter="tag:gallery" limit="24" include="tags" fields="title,feature_image,slug,url,excerpt"}}
    {{#foreach posts}}
      {{#if feature_image}}
        <figure class="gallery-item">
          <a href="{{url}}" class="gallery-link">
            <img src="{{img_url feature_image size="m"}}" alt="{{title}}" loading="lazy" />
            <figcaption>
              <h3>{{title}}</h3>
              {{#if primary_tag}}
                <span class="category-tag">{{primary_tag.name}}</span>
              {{/if}}
            </figcaption>
          </a>
        </figure>
      {{/if}}
    {{/foreach}}
    
    {{#unless posts}}
      {{!-- Fallback message if no gallery posts exist --}}
      <div class="gallery-empty">
        <p>No fish pics yet. We're too busy catching them to photograph them. Check back soon!</p>
      </div>
    {{/unless}}
  {{/get}}
</div>

{{!-- Instagram CTA --}}
<div class="gallery-cta">
  <p>Follow us on the 'gram for daily doses of fish content and bad puns</p>
  <a href="https://instagram.com/flowstatefishingllc" target="_blank" rel="noopener noreferrer" class="btn btn-outline-light">
    <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
      <rect x="2" y="2" width="16" height="16" rx="4" stroke="currentColor" stroke-width="1.5"/>
      <circle cx="10" cy="10" r="3" stroke="currentColor" stroke-width="1.5"/>
      <circle cx="14.5" cy="5.5" r="1" fill="currentColor"/>
    </svg>
    @flowstatefishingllc
  </a>
</div>
</content>
</invoke>